{
  "name": "@fen-web/integration",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test \"dist/**/*.test.js\"",
    "e2e:codex": "tsc -p tsconfig.json && node dist/e2e-codex.mjs"
  },
  "dependencies": {
    "@fen-web/runtime": "*",
    "@fen-web/bindings": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  }
}
